# random-accnumber-generator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build random-accnumber-generator` to build the library.

## Running unit tests

Run `nx test random-accnumber-generator` to execute the unit tests via [Jest](https://jestjs.io).
